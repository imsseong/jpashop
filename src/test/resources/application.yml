spring:
#  datasource:
#    url: jdbc:h2:mem:test #test클래스는 메모리 DB 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop #drop query까지 날려
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: 줌true
#별도의 설정이 없으면 spring boot는 메모리 모드로 돌림
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace